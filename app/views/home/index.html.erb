<!-- Page Header -->
<!-- Set your background image for this header on the line below. -->
<header class="intro-header" style="background-image: url('<%= image_url('home-bg.jpg')%>')">
    <div class="site-heading">
        <h1>ZZULU</h1>
        <hr class="small">
        <span class="subheading">A simple little idea that will change everything.</span>
    </div>
</header>

<section id="about">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 text-center">
                <img class="img-responsive" src="<%= image_url('junwoo.png')%>" width="300px" alt>
                <h2 class="section-heading">황준우<small> Hwang Junwoo</small></h2>
                <hr class="small primary">
                <p class="text-faded">
                    경북대학교 공과대학 신소재공학부 전자재료전공 / IT대학 컴퓨터학부 부전공<br/>
                    멋쟁이 사자처럼 / ifnill<br/>
                    GIF2015 빅데이터 부문 3위 (Team 'ifnill')<br/>
                    Android / HTML / CSS / JavaScript / Ruby on Rails<br/>
                </p>
                <% unless current_user.nil? %>
                    <% if current_user.admin==true %>
                        <%= button_to "Add", new_portfolio_path, method: :get, class: "btn btn-default" %>
                    <% end %>
                <% end %>
            </div>
        </div>
    </div>
</section>

<!-- <section id="services">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="section-heading">PORTFOLIO</h2>
                <hr class="small primary">
            </div>
        </div>
    </div>
</section> -->


<section class="no-padding" id="portfolio">
    <div class="container-fluid">
        <div class="row no-gutter">

        <% @portfolios.each do |p| %>
            <!-- do -->
            <div class="col-lg-4 col-sm-6">
                <a href="#portfolioModal_<%= p.id %>" class="portfolio-box" data-toggle="modal">
                    <img src="<%= p.image.url %>" class="img-responsive" alt="<%= p.title %>">
                    <div class="portfolio-box-caption">
                        <div class="portfolio-box-caption-content">
                            <div class="project-category text-faded">
                                <%= p.category %>
                            </div>
                            <div class="project-name">
                                <%= p.title %>
                            </div>
                        </div>
                    </div>
                </a>
            </div>

                <!-- Portfolio Modals -->
                <div class="portfolio-modal modal fade" id="portfolioModal_<%= p.id %>" tabindex="-1" role="dialog" aria-hidden="true">
                    <div class="modal-content">
                        <div class="close-modal" data-dismiss="modal">
                            <div class="lr">
                                <div class="rl">
                                </div>
                            </div>
                        </div>
                        <div class="container">
                            <div class="row">
                                <div class="col-lg-8 col-lg-offset-2">
                                    <div class="modal-body">
                                        <h2><%= p.title %></h2>
                                        <hr class="small primary">
                                        <img src="<%= p.image.url %>" class="img-responsive img-centered" alt="">
                                        <p>
                                            <%= p.context.gsub("\r\n","<br>").html_safe %>
                                        </p>
                                        <ul class="list-inline item-details">
                                            <li>Category:
                                                <strong>
                                                    <%= p.category %>
                                                </strong>
                                            </li>
                                            <li>Date:
                                                <strong>
                                                    <%= p.date %>
                                                </strong>
                                            </li>
                                            <li>Service:
                                                <strong>
                                                    <%= p.service %>
                                                </strong>
                                            </li>
                                        </ul>

                                        <% unless current_user.nil? %>
                                            <% if current_user.admin==true %>
                                                <%= button_to "Edit", "/portfolio/#{p.id}/edit", method: :get, class: "btn btn-primary" %>
                                                <%= button_to "Delete", "/portfolio/#{p.id}", data: { confirm: "삭제합니다." }, method: :delete, class: "btn btn-danger" %>
                                            <% end %>
                                        <% end %>

                                        <button type="button" class="btn btn-default" data-dismiss="modal"><i class="fa fa-times"></i> Close</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            
            <% end %>
            <!-- end -->
        </div>
    </div>
</section>